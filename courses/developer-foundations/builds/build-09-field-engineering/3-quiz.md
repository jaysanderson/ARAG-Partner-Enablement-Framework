# Build 9 — Quick Quiz: Field Engineering

> 5 multiple-choice. Open-book. Pass = 4/5.

---

### 1. Field engineering aligns which two contracts?

A. Schema and classifier.
B. Content fields and prompt instructions.
C. Auth and observability.
D. Retrieval and generation.

---

### 2. How does the model consume a `callToAction` field?

A. As a special body parameter on `/ask`.
B. Auto-detected by field name.
C. Inside `{context}`, referenced by the prompt.
D. Via a built-in widget renderer.

---

### 3. How do you A/B test a CTA without a code deploy?

A. Switch to a different LLM.
B. Edit the field value in the dashboard.
C. Modify the front-end prompt config.
D. Recompile the React app.

---

### 4. `searchResultDisplay` is typically stored as:

A. A flag enabling search results.
B. A JSON object stored as a string field.
C. A boolean visibility setting.
D. A list of supported icons.

---

### 5. The commercial pitch for field engineering is:

A. One-off implementation work.
B. Hosting fees for field definitions.
C. Recurring content-engineering retainer.
D. A percentage of conversion uplift.

---

## Answer key

1. B · 2. C · 3. B · 4. B · 5. C

4+ correct → pass. Continue to [Build 10](../build-10-composite-rag/).
