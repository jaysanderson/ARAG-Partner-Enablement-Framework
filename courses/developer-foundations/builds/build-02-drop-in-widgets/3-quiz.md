# Build 2 — Quick Quiz: Drop-in Widgets

> 5 multiple-choice. Open-book. Pass = 4/5.

---

### 1. The Progress Agentic RAG widget library ships as:

A. A React component package on npm
B. Standard Web Components from a CDN script
C. A jQuery plugin
D. An iframe embed

---

### 2. To restrict a `<nuclia-search-bar>` to videos only, the attribute is:

A. `content-type="video"`
B. `filter="video"`
C. `filters='["/icon/video"]'`
D. `restrict="application/video"`

---

### 3. The Progress Agentic RAG widget configurator's three panes are:

A. Editor, terminal, browser
B. Configuration, live preview, generated code
C. Search, chat, analytics
D. Filters, deploy, logs

---

### 4. A CMO wants a chatbot on WordPress with no engineering. The right answer is:

A. No, this requires a Tier 3 engagement.
B. Yes, paste a few script tags and a custom element.
C. Yes, but only after a four-week React build.
D. Yes, but only via iframe embed.

---

### 5. The widgets are the wrong choice when:

A. The customer is small.
B. The corpus has fewer than 100 documents.
C. The customer requires EU residency.
D. Different audiences need different prompt voices.

---

## Answer key

1. B · 2. C · 3. B · 4. B · 5. D

4+ correct → pass. Continue to [Build 3](../build-03-conversational-surfaces/).
